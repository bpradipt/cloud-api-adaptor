[Unit]
# Pull in the image cache setup when it is present in the image.
# Using Wants= (not Requires=) so kata-agent still starts if the service fails.
Wants=kata-image-cache.service
After=kata-image-cache.service
